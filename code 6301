# 항목 값으로는 0을 갖는 2*3*4 형태의 3차원 배열을 생성하는

# 리스트 내포 기능을 이용한 프로그램을 작성하십시오.

a_list = list(map(int,input().split(',')))
arr = [[[0]*a_list[2] for _ in range(int(a_list[1]))] for _ in range(int(a_list[0]))]

print(arr) -> Runtime error 뜸
-----------------------------------------------------------------

arr = [[[0 for _ in range(4)] for _ in range(3)] for _ in range(2)]
print(arr)
